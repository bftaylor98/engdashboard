# Copy to .env.chart and fill in. Auth from app: server/database/init.js (default users), server/routes/auth.js
# Default users: rob, damien, thad, alex, phillip, brad, mike, admin. Default password: changeme
DASHBOARD_API_URL=http://localhost:3001
DASHBOARD_USER=admin
DASHBOARD_PASSWORD=changeme
